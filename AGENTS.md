# AGENTS.md — AI 协作指南

本文件供 AI 编码助手阅读，明确 AI 编程约定与必须遵守的硬约束。

---

## 1. 项目概述

DJI Dock 模拟器/监控器：模拟 DJI Dock（Dock1/Dock2/Dock3）机场及其配套飞行器（M30/M3D/M4D 系列）的上云注册、设备上线、OSD/State 属性上报、航线任务、直播、媒体上传、HMS 告警等完整云端交互流程，对接巡飞平台（hivemind，DJI Cloud API 后端）。

> 项目价值（设备模拟 + 调试工具）见 [设计文档 §1](docs/superpowers/specs/2026-08-08-dji-dock-simulator-design.md)。核心：比真机更快捷地验证平台代码正确性，所有设计决策围绕此价值展开。

- **后端**：Java Spring Boot，MQTT（Paho v3）连接 EMQX/hivemind
- **前端**：Vue 3 + Element Plus（`src/main/resources/static/index.html` 模拟器、`monitor.html` 监控器）
- **桌面端**：Tauri 打包（端口 9090 / Tauri 固定 19090,若遇端口占用，自动加1）
- **配置**：`application.yml` → `SimulatorProperties` → `RuntimeConfig`（启动时 `LiveConfigStore` 加载持久化的 live 配置覆盖默认值）→ 前端 REST API 覆盖（变更时自动持久化）

> 设计细节（架构、DJI 时序图、协议覆盖、数据流、错误码体系、update_topo 核实结论等）见 [设计文档](docs/superpowers/specs/2026-08-08-dji-dock-simulator-design.md)。
> 规格测试用例（容易搞错的陷阱、TDD 开发模式）见 [TDD 规格测试文档](docs/TDD-SPEC.md)。

---

## 2. AI 必须遵守的硬约束

### MQTT 连接
- MQTT 连接仅在用户点击「注册到第三方平台」后建立，Spring 启动时不自动连接
- 开机时若上次注册成功（`localStorage.registered=true`）才尝试自动重连
- 关机时 MQTT 客户端必须断开
- 连接超时 3 秒
- 连接失败需区分：地址错误(-4)提示「无法链接到第三方平台」；凭证错误(-5)提示「第三方平台凭证有误」

### 注册流程
- 时序：`config → airport_bind_status → airport_organization_get → airport_organization_bind`（注册成功后 update_topo 上线）
- 每一步指令无条件执行，不根据绑定状态跳过
- 绑定码错误（result=210229）停止注册，提示「组织ID与绑定码错误」
- config 请求超时重试 3 次（间隔 3 秒），全失败才停止注册
- config 回复的 `app_license` 需与本地配置比对一致，不一致停止注册返回 -6
- update_topo 对齐 DJI 行为：超时不停止上线流程

### 工程约束
- 无硬编码值，可配参数走 `application.yml` → `SimulatorProperties` → `RuntimeConfig`
- 业务逻辑返回明确拒绝原因而非抛异常（HTTP 200 + success=false + message）
- Vue DOM 模板中自定义元素（`<el-xxx>`）必须用显式闭合标签，禁止自闭合
- 不适用的值用 `-` 显示，不用 `0` 或 `--`
- 大文件拆分遵循以下原则：
  - **单一职责触发**：文件承担多个独立职责（存在多个不相关的变更原因）时拆分。如 OSD 字段构造与 MQTT 发布机制分离——字段集随设备类型变化，发布机制稳定
  - **认知复杂度优先**：不单看行数，关键看抽象层级数量。单文件包含 3+ 个抽象层级（如调度 + 构造 + 序列化 + 辅助方法）时考虑拆分
  - **变化频率隔离**：频繁变化的逻辑与稳定逻辑分离，避免修改波动扩散。如字段集独立为 Builder，发布机制留在调度器
  - **高内聚低耦合**：拆分边界保证文件内高内聚（相关逻辑在一起）、文件间低耦合（依赖通过接口/参数传递，不通过共享可变状态）
  - **避免过度拆分**：相关逻辑保持在一起，不为行数指标强行拆分。判断标准：拆分后是否需要频繁跨文件查看才能理解一个完整流程——如果是，说明拆分过度
  - **AI 协作友好**：单文件控制在 AI 可高效读取修改的范围（建议 200-400 行），避免 AI 因上下文限制遗漏关联逻辑或引入不一致修改
  - **测试可隔离**：拆分后各模块可独立测试，符合 TDD 模式中"测试用例聚焦单一行为"的要求

---

## 3. AI 代码修改前置流程约定

1. **先读后改**：修改任何文件前必须先 Read 理解现有代码，不在未读文件上提建议
2. **分析先行**：复杂任务先分析方案、比较取舍，与用户达成一致后再编码
3. **依赖官方文档**：涉及 DJI 协议的问题必须查阅 DJI Cloud API 官方文档，不得凭猜测下结论
4. **共性优先**：遇到问题先分析是否为同类共性问题，架构优化能解决的优先提供优化建议（经用户确认后执行），善用设计模式，不打补丁式修改
5. **配置无硬编码**：新增可配参数走 yml → Properties → RuntimeConfig 链路
6. **编译验证**：Java 改动后执行 `mvn -q compile -DskipTests` 验证
7. **修改后文档对齐**：每次代码修改完成后，必须核对改动是否符合 DJI Cloud API 官方文档（https://developer.dji.com/doc/cloud-api-tutorial/cn/）规定，发现偏差立即更正
8. **价值对齐**：每次设计或实现决策必须服务于"更快验证平台代码正确性"的核心价值，避免脱离调试工具定位的过度设计
9. **TDD 驱动**：新增功能或修复 Bug 时，先在 [TDD 规格测试文档](docs/TDD-SPEC.md) 编写测试用例（基于 DJI 文档规格，非实现代码），再实现代码；优化阶段以该文档为依据确保不破坏已有行为
10. **方案审查**：开发者提供修改方案时，AI 必须先分析方案的自洽性（内部是否矛盾）、合理性（是否符合架构设计和 DJI 文档）、危害性（是否会破坏现有功能或引入新问题），不能盲目按方案立即修改代码。开发者拥有方案选择权，方案分析由 AI 负责
11. **错误代码即删**：确定为错误的设计和实现，注解说明原因后立即删除，不保留注释标记的废弃代码，避免 AI 后续优化时将错误代码改回
12. **架构演进同步**：架构升级导致原有类名、方法名、变量名、文档描述失去准确含义时，必须及时更新命名和表述，避免历史命名误导后续 AI 决策。判断标准：当前名称是否仍能准确描述其职责——如果不能（如项目名"DJI Dock3 模拟器"实际已支持 Dock1/Dock2/Dock3），必须更正。涵盖范围：代码命名、文档表述、配置字段名、注释说明

---

## 4. 修改结果与影响分析汇报

每次完成代码修改后，AI 必须汇报：

- **修改清单**：列出改动的文件及具体位置（带文件链接）
- **修改内容**：每个文件改了什么、为什么改
- **影响分析**：改动对现有功能的影响（是否破坏既有流程、是否影响其他模块）
- **文档对齐核实**：每次修改必须核对是否符合 DJI 官方文档，注明依据的文档 URL 及关键原文（涉及协议改动时尤其严格）
- **验证结果**：编译/测试状态
- **待确认项**：如有不确定的影响或需用户决策的点，明确列出

---

## 5. 文档更新策略

四类文档各有单一职责，内容不重复，通过引用关联：

| 文档 | 职责 | 真相源范围 |
|---|---|---|
| README.md | 用户手册 + 开发导航 | 配置说明、操作指南、联调步骤、项目结构概览、文档索引 |
| 设计文档 | 设计详情定义源 | 架构、时序图、协议、数据流、错误码、核实结论、技术选型 |
| AGENTS.md | AI 编程约定 | 修改流程、硬约束、汇报要求、文档更新策略、TDD 模式 |
| TDD-SPEC.md | 规格测试用例 | 容易搞错的陷阱、Given-When-Then 测试用例 |

### 更新时机

| 场景 | 更新顺序 | 更新内容 |
|---|---|---|
| 新增功能 | TDD-SPEC.md → 设计文档 → 代码 | 先写测试用例，再更新设计，再实现 |
| 修复 Bug | TDD-SPEC.md → 代码 | 添加回归测试用例，再修复 |
| 协议变更 | 设计文档 → TDD-SPEC.md → 代码 | 先更新时序图/协议，再更新测试用例，再实现 |
| 配置变更 | 设计文档 → README.md | 设计文档更新定义，README.md 同步用户配置说明 |
| AI 约定变更 | AGENTS.md | 更新修改流程/硬约束/策略 |
| 项目结构变更 | 设计文档 → README.md | 设计文档更新详细版，README.md 更新导航概览 |
| 优化重构 | TDD-SPEC.md（核对）→ 代码 | 以测试用例为依据，确保不破坏已有行为 |

### 更新原则

1. **TDD 先行**：任何功能/Bug 变更，先更新 TDD-SPEC.md 测试用例
2. **单一真相源**：每类内容只在一个文档中定义，其余引用
   - 项目价值/时序/协议/错误码 → 设计文档
   - AI 约定/文档策略 → AGENTS.md
   - 规格行为/陷阱 → TDD-SPEC.md
   - 用户操作/配置 → README.md
3. **不跨文档复制**：通过链接引用，不粘贴重复内容
4. **修改后核对**：按 §3 第7条文档对齐要求，核对改动是否符合 DJI 官方文档
